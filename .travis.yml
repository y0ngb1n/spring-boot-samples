language: java
sudo: false

addons:
  sonarcloud:
    organization: "y0ngb1n"
    token:
      secure: "$SONAR_TOKEN"

jdk:
  - openjdk8

script:
  - mvn clean org.jacoco:jacoco-maven-plugin:prepare-agent install sonar:sonar

cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.sonar/cache'
